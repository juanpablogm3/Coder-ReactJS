import React from "react";
import ShoppingCartIcon from "@mui/icons-material/ShoppingCart";

const CartWidget = () => {
    return <ShoppingCartIcon color="primary" fontSize="large" />
}

export default CartWidget;